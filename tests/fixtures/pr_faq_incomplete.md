# Incomplete on purpose

This file is the positive control for `tests/test_pr_faq_format.py`.
It must stay incomplete — if the checker starts accepting it, the guard is dead.

## Headline

A heading that exists so the file looks like a start.

## Problem

Only two of the required headings are here.
