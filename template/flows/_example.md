---
title: Example Flow
reads: []
writes: [state/notes/example-last.md]
think_aloud: auto
model: default
---

# Example Flow

Skeleton Turtle Practice program. Replace body with your prompt program.

When this flow runs in an eddy, the shell loads `reads` before dialogue and may write to `writes` when the flow completes or checkpoints.

Plain markdown without front matter can still run — it just won't touch persistent state.
